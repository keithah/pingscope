---
phase: 11-11
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/PingScope/Views/Settings/HostSettingsView.swift
  - .planning/phases/*/*-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "Legacy unused settings view is removed so active settings path is unambiguous"
    - "Planning summaries use consistent `Sources/PingScope/...` file references"
  artifacts:
    - path: "Sources/PingScope/Views/Settings/HostSettingsView.swift"
      provides: "Legacy view removed from tree"
      contains: "(deleted)"
    - path: ".planning/phases/*/*-SUMMARY.md"
      provides: "Consistent key-file path naming for audit traceability"
      contains: "Sources/PingScope/"
  key_links:
    - from: ".planning/v1.0-v1.0-MILESTONE-AUDIT.md"
      to: ".planning/phases/*/*-SUMMARY.md"
      via: "path naming cleanup"
      pattern: "Sources/PingMonitor/"
---

<objective>
Clean up remaining structural debt: remove dead settings UI and normalize planning summary source-path references.

Purpose: Reduce maintenance confusion and improve audit traceability after v1.0 debt review.
Output: Dead legacy settings file removed and summary docs consistently referencing `PingScope` paths.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
@.planning/phases/11-11/11-RESEARCH.md

@Sources/PingScope/Views/Settings/HostSettingsView.swift
@.planning/phases/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy HostSettingsView</name>
  <files>Sources/PingScope/Views/Settings/HostSettingsView.swift</files>
  <action>Delete `HostSettingsView.swift`, which is not referenced by active runtime settings flows. Before deletion, confirm there are no remaining compile references. Do not modify active settings components (`PingMonitorSettingsView`, `NotificationSettingsView`) except for required import/reference cleanup if the compiler requires it.</action>
  <verify>`swift build --build-tests` passes after deletion.</verify>
  <done>Unused `HostSettingsView` no longer exists and build remains green.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize summary path conventions</name>
  <files>.planning/phases/*/*-SUMMARY.md</files>
  <action>Update historical summary docs to use `Sources/PingScope/...` path references wherever they still use legacy `Sources/PingMonitor/...` naming. Keep changes strictly textual and path-focused (no semantic rewrites). Use scripted replacement for consistency and verify no legacy path tokens remain in phase summaries.</action>
  <verify>`rg "Sources/PingMonitor/" .planning/phases/*/*-SUMMARY.md` returns no matches.</verify>
  <done>Planning summaries consistently reference `PingScope` source paths for traceability.</done>
</task>

</tasks>

<verification>
- `swift build --build-tests`
- `rg "Sources/PingMonitor/" .planning/phases/*/*-SUMMARY.md` (expect no output)
</verification>

<success_criteria>
1. `HostSettingsView.swift` is removed without breaking build.
2. Summary docs no longer mix `PingMonitor`/`PingScope` source path naming.
3. Phase audit traceability no longer reports path convention mismatch.
</success_criteria>

<output>
After completion, create `.planning/phases/11-11/11-03-SUMMARY.md`
</output>
