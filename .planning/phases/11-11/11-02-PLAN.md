---
phase: 11-11
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/PingScope/MenuBar/NotificationPreferencesStore.swift
  - Sources/PingScope/Views/Settings/PingMonitorSettingsView.swift
  - Sources/PingScope/Views/Settings/HostNotificationOverrideEditorView.swift
  - Tests/PingScopeTests/NotificationPreferencesStoreTests.swift
autonomous: true

must_haves:
  truths:
    - "Users have an active settings path to configure per-host notification overrides"
    - "Per-host override edits persist and are loaded from NotificationPreferencesStore"
    - "Hosts can inherit global alert-type settings when no override is configured"
  artifacts:
    - path: "Sources/PingScope/Views/Settings/HostNotificationOverrideEditorView.swift"
      provides: "UI for host-level enable/disable and alert-type overrides"
      contains: "AlertType"
    - path: "Sources/PingScope/Views/Settings/PingMonitorSettingsView.swift"
      provides: "Entrypoint wiring from active settings host list to override editor"
      contains: "sheet"
    - path: "Sources/PingScope/MenuBar/NotificationPreferencesStore.swift"
      provides: "Store helpers for read/write/clear host override state"
      contains: "hostOverride"
    - path: "Tests/PingScopeTests/NotificationPreferencesStoreTests.swift"
      provides: "Persistence behavior coverage for host override helpers"
      contains: "hostOverride"
  key_links:
    - from: "Sources/PingScope/Views/Settings/PingMonitorSettingsView.swift"
      to: "Sources/PingScope/Views/Settings/HostNotificationOverrideEditorView.swift"
      via: "host row action + sheet presentation"
      pattern: "HostNotificationOverrideEditorView"
    - from: "Sources/PingScope/Views/Settings/HostNotificationOverrideEditorView.swift"
      to: "Sources/PingScope/MenuBar/NotificationPreferencesStore.swift"
      via: "load/save override actions"
      pattern: "setHostOverride"
---

<objective>
Expose `NotificationPreferences.hostOverrides` through the active Settings experience.

Purpose: Close the v1.0 debt item where host override persistence existed but users had no active UI path to configure it.
Output: Host-level notification override editor reachable from current settings shell with persistence-backed save/reset behavior.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
@.planning/phases/11-11/11-RESEARCH.md
@.planning/phases/06-notifications-settings/06-01-SUMMARY.md
@.planning/phases/06-notifications-settings/06-07-SUMMARY.md

@Sources/PingScope/Models/NotificationPreferences.swift
@Sources/PingScope/MenuBar/NotificationPreferencesStore.swift
@Sources/PingScope/Views/Settings/PingMonitorSettingsView.swift
@Sources/PingScope/Views/Settings/NotificationSettingsView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend NotificationPreferencesStore host override APIs</name>
  <files>Sources/PingScope/MenuBar/NotificationPreferencesStore.swift</files>
  <action>Add focused helpers for host override workflows used by Settings: read host override with sensible default state, save override, clear override (revert to global), and optional convenience for host-level effective alert types. Keep persistence format unchanged (`NotificationPreferences` payload), and do not duplicate business rules already enforced in `NotificationService`.</action>
  <verify>`swift build --build-tests` passes.</verify>
  <done>Store exposes stable host override read/write/reset APIs suitable for direct UI binding.</done>
</task>

<task type="auto">
  <name>Task 2: Add host-level override editor in active Settings flow</name>
  <files>Sources/PingScope/Views/Settings/PingMonitorSettingsView.swift, Sources/PingScope/Views/Settings/HostNotificationOverrideEditorView.swift</files>
  <action>Create a dedicated host notification override editor view and wire it from host rows in `PingMonitorSettingsView`. Editor must allow: host notifications enabled/disabled, optional per-host alert-type override, and reset-to-global behavior. Use the existing shared `notificationStore` instance from active settings path (do not revive legacy `HostSettingsView`). Keep current Hosts/Notifications/Display tab structure intact.</action>
  <verify>`swift build --build-tests` passes and UI compiles without introducing a second settings shell.</verify>
  <done>Users can open a host-specific override editor from active Settings and persist override changes.</done>
</task>

<task type="auto">
  <name>Task 3: Add persistence tests for host override helper behavior</name>
  <files>Tests/PingScopeTests/NotificationPreferencesStoreTests.swift</files>
  <action>Create targeted tests for host override persistence semantics (save, load, remove/reset). Use isolated `UserDefaults` test suite keys to avoid cross-test leakage. Cover both explicit override cases and fallback/no-override case.</action>
  <verify>`swift test --filter NotificationPreferencesStoreTests` passes.</verify>
  <done>Host override persistence behavior is regression-tested and deterministic.</done>
</task>

</tasks>

<verification>
- `swift build --build-tests`
- `swift test --filter NotificationPreferencesStoreTests`
</verification>

<success_criteria>
1. Active settings UI provides a direct per-host override configuration path.
2. Host override edits persist in `NotificationPreferences.hostOverrides` and reload correctly.
3. Users can reset a host to inherit global notification alert-type configuration.
</success_criteria>

<output>
After completion, create `.planning/phases/11-11/11-02-SUMMARY.md`
</output>
