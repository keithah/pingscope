---
phase: 12-icmp-host-persistence-verification-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/PingScope/ViewModels/AddHostViewModel.swift
  - Sources/PingScope/Services/HostStore.swift
  - Tests/PingScopeTests/HostStoreTests.swift
autonomous: true

must_haves:
  truths:
    - "ICMP hosts can be added/edited through active host CRUD without failing persistence validation"
    - "Non-ICMP host methods still require valid non-zero ports"
  artifacts:
    - path: "Sources/PingScope/ViewModels/AddHostViewModel.swift"
      provides: "Host build semantics for method-aware effective port resolution"
      contains: "effectivePort"
    - path: "Sources/PingScope/Services/HostStore.swift"
      provides: "Method-aware host validation for add/update persistence"
      contains: "isValidHost"
    - path: "Tests/PingScopeTests/HostStoreTests.swift"
      provides: "Regression tests for ICMP vs non-ICMP port validation"
      contains: "icmp"
  key_links:
    - from: "Sources/PingScope/ViewModels/AddHostViewModel.swift"
      to: "Sources/PingScope/Services/HostStore.swift"
      via: "built host payload passed into add/update"
      pattern: "effectivePort"
    - from: "Sources/PingScope/Services/HostStore.swift"
      to: "Tests/PingScopeTests/HostStoreTests.swift"
      via: "validation behavior regression assertions"
      pattern: "isValidHost"
---

<objective>
Fix the ICMP host CRUD persistence break by aligning host construction and host-store validation semantics.

Purpose: Close the blocker where ICMP hosts are built with port `0` but rejected before persistence, preventing HOST-11 from being satisfied end-to-end.
Output: ICMP host add/edit payloads are accepted and persisted while non-ICMP validation rules remain strict.
</objective>

<execution_context>
@/Users/keith/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/keith/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
@.planning/phases/12-icmp-host-persistence-verification-closure/12-RESEARCH.md
@.planning/phases/10-true-icmp-support/10-03-SUMMARY.md

@Sources/PingScope/ViewModels/AddHostViewModel.swift
@Sources/PingScope/Services/HostStore.swift
@Sources/PingScope/Models/PingMethod.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align method-aware port semantics across host construction and validation</name>
  <files>Sources/PingScope/ViewModels/AddHostViewModel.swift, Sources/PingScope/Services/HostStore.swift</files>
  <action>Update validation logic so `.icmp` hosts are considered valid with `port == 0`, while `.tcp`, `.udp`, and `.icmpSimulated` continue requiring `port > 0`. Keep existing required-field checks (`name`, `address`) intact. Avoid broad relaxations that permit invalid non-ICMP ports.</action>
  <verify>`swift build --build-tests` succeeds with no new compile errors.</verify>
  <done>Adding or editing an ICMP host no longer fails at `HostStore` validation, and non-ICMP validation behavior is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for HostStore ICMP validation behavior</name>
  <files>Tests/PingScopeTests/HostStoreTests.swift</files>
  <action>Create focused actor-safe tests that prove: (1) ICMP host with `port 0` is accepted by `add`/`update`, (2) non-ICMP host with `port 0` is still rejected, and (3) valid non-ICMP host persists as before. Use isolated `UserDefaults` suites so tests do not leak state.</action>
  <verify>`swift test --filter HostStoreTests` passes.</verify>
  <done>Regression tests protect the exact ICMP persistence bug path and guard against over-permissive validation changes.</done>
</task>

</tasks>

<verification>
- `swift build --build-tests`
- `swift test --filter HostStoreTests`
</verification>

<success_criteria>
1. ICMP hosts built by active CRUD flow can be persisted by `HostStore`.
2. TCP/UDP/ICMP-simulated hosts still require non-zero ports.
3. New regression tests fail if ICMP/non-ICMP validation semantics regress.
</success_criteria>

<output>
After completion, create `.planning/phases/12-icmp-host-persistence-verification-closure/12-01-SUMMARY.md`
</output>
